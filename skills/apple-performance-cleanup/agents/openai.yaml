interface:
  display_name: "Apple Performance Cleanup"
  short_description: "Performance-safe cleanup and codebase compaction."
  default_prompt: "Use $apple-performance-cleanup to simplify this performance-sensitive Apple app path or codebase without behavior or performance drift."
