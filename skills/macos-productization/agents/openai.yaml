interface:
  display_name: "macOS Productization"
  short_description: "macOS release and distribution artifact proof."
  default_prompt: "Use $macos-productization to prepare or verify this macOS release artifact for its intended distribution channel."
